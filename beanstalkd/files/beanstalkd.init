#!/bin/sh /etc/rc.common
# Beanstalk
 
START=91
STOP=09
PIDFILE=/var/run/beanstalkd.pid
 
start() {
    (/usr/bin/beanstalkd && echo $! > "$PIDFILE") &
}
 
stop() {
    kill $(cat "$PIDFILE")
}

restart() {
    stop
    start
}

