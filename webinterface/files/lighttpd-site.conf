
$HTTP["host"] =~ "^(enigma\.box|box)$" {

    include_shell "cat auth/webinterface.conf"

    #TODO: upload size 120M

    proxy.server = ( "" =>
        ( (
            "host" => "127.0.0.1",
            "port" => 8000
        ) )
    )

}

